import { ShowTopQuoteDirective } from './show-top-quote.directive';

describe('ShowTopVoteDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowTopQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
